# 仓库审查：双语文档与素材输入一致性

审查日期：2026-09-12。此次更新集中在 README、复现说明和准备脚本；Pink Sakura 的源图、模型、纹理及历史验证证据没有改动。

## 文档交付

- [中文 README](../README.md) 与 [English README](../README.en.md) 提供语言切换、模型图片表格、超分配置和复建入口。
- [头图](../assets/readme/README.md) 是另行生成的宣传插图，单独记录来源与 CC BY 4.0 范围；表格中的运行截图保留原始验证文件。
- Humanizer-zh 与 Humanizer 用于中英文文字审校。技术事实、命令和模型限制按实际记录保留，工具本身没有替代事实验证。
- [超分权重锁](../tools/upscale-model-lock.json) 保存 `realesr-animevideov3-x4` 的固定上游提交、下载地址、文件大小和 SHA-256。两份下载文件与历史生产记录完全一致；没有把权重纳入 Git。

## 找到并修复的问题

| 问题与触发方式 | 修复后的行为 | 验证方式 |
| --- | --- | --- |
| HTML 图片表格使用 `<img src>`，旧的 kit 链接检查只覆盖 Markdown 链接，漏掉头图等资源 | Markdown 文档中的 HTML `src`、`href`、`poster` 一并检查；排除代码示例 | 缺失文件、编码路径和代码占位符回归；中英文实际本地渲染 |
| 模型资源写成 `./model.model3.json`，绕过字面名称保护；复制后被预览入口覆盖 | 复制前统一规范化目标路径并按不区分大小写检查，拒绝别名覆盖及文件/目录冲突 | 覆盖负例提前退出；正常嵌套资源逐字节保留 |
| NCNN 推理期间低分图集被另一导出修改，RGB 和 alpha 可能来自不同版本，记录的 SHA 也会偏离实际输入 | 推理前冻结全部源页，RGB 与 alpha 消费同一快照；结束前复查原文件，变化则失败，`complete=false`，不生成可复用的 `manifest-hd.json` | 双页源图改变、推理失败和成功路径回归；实际 Java 配合模拟 NCNN 重现旧问题，验证失败保护 |

这些修复保护资源与记录的一致性，不改变角色画稿、Alpha 缩放算法或网格绑定。超分仍需使用新的空输出目录；对错误拆层的修复仍在超分前完成。

## 验证范围

本地 Python 回归共 **41 项通过**。其中外部推理与 SDK 的模拟夹具只验证脚本行为，不作为神经超分或 Core 成功的证据。

中英文 README 用本地 Marked/Mermaid 和 Chromium 分别以 1440 px、390 px 宽度渲染。四种页面均成功加载本地图片，Mermaid 无解析错误，页面整体没有横向溢出；桌面头图、图片表格和窄屏表格另做目视检查。这是本地 Markdown 展示检查，不是 GitHub 在线渲染验收。

### 修复后的真实运行回归

公开 Java 生成器重新产生 14 层几何素材，在全新输出目录完成 LOW 导出、神经超分、HD 导出和预览。此轮复用了制作机已有编译类与 JAR；不是空缓存源码构建。可机读的测量结果见 [回归摘要](repository-review-results.json)。

| 阶段 | 实际结果 |
| --- | --- |
| 真实 Upscayl NCNN | `realesr-animevideov3-x4`，1024² → 4096²；固定源页快照 SHA 与 LOW 图集一致，`complete=true` |
| LOW / HD MOC | 逐字节一致，SHA-256：`46213ca7f0523902a42f307a49f037c205eecd2e43b1726ee87dd627ed9c8aa9` |
| 官方 Native Core 6.0.257 | LOW 与 HD 均通过；19 个参数、16 个 Drawable、2996 个顶点 |
| 实际浏览器 | Chrome 152.0.7977.83、Playwright 1.63.0、Web Core 5.1.0；22 项检查通过，实际载入文件 SHA 匹配准备清单 |
| 参数效果 | 17 个参数观察到几何/透明度变化；最小示例没有眉毛素材，`ParamBrowLY` / `ParamBrowRY` 无可见绑定 |
| 设备与服务 | 0 次摄像头请求；仅启动并关闭本轮独立预览服务 |

[公开源包复现审查](reproducibility.md) 记录 `git archive` 的独立检查。源图与配方不依赖作者旧的 `work/`；完整空缓存引擎构建在 Gradle 下载阶段主动停止，尚未完成，不能用历史编译产物的导出报告替代。

VTube Studio 验收仍交由用户，本次没有调用摄像头，也没有修改用户正在使用的角色预览服务。
